# Heroku buildpack for deployer dependencies

This is a [Heroku buildpack](http://devcenter.heroku.com/articles/buildpacks) that installs binaries and libraries
that are required for the deployer to function.

## Current executables
* AWS
* gnu tar
* Heroku
